<div class="container m-4">
  <h2 class="mt-5 mb-5 ml-4"> Echa un vistazo a los cursos disponibles</h2>
  <p class="m-4 fs-5">
    Elige entre más de 100 cursos online para aprender desde cero o desarrollar tus talento musical <br/>
  </p>
  <div class="container mx-auto mt-4">
    <div class="row" style="gap: 4rem 0">
      <% @courses.each do |curse| %>
        <%= render curse %>
      <% end %>
    </div>
    <% if @courses.length == 0 %>
    <%= image_tag "cat.png" %>
      <p>No existen cursos publicados.</p>
    <% end %>
  </div>
</div>
