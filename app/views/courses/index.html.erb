<div class="container mx-auto m-4">
  <h2 class="mx-2 mt-5 ml-4"> Echa un vistazo a los cursos disponibles</h2>
  <p class="mx-2 mb-3 fs-5">
    Aprende desde cero y desarrolla tu talento musical <br/>
  </p>
  <div class="container pt-4 mt-4 border-top border-4">
    <div class="row" style="gap: 4rem 0">
      <% @courses.each do |curse| %>
        <%= render curse %>
      <% end %>
    </div>
    <% if @courses.length == 0 %>
      <p>No existen cursos publicados.</p>
    <% end %>
  </div>
</div>
